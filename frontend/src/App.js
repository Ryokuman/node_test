import { Button } from "./components/Button";

function App() {
  return (
    <div>
      <Button core={1} />
      <Button core={2} />
      <Button core={3} />
      <Button core={4} />
    </div>
  );
}

export default App;
